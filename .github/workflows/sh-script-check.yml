name: Shell Script Check
run-name: Shell Script Check - ${{ github.actor }} on ${{ github.ref_name }}

on:
    workflow_call:

jobs:
    shellcheck:
        runs-on: ubuntu-latest

        steps:
        - uses: actions/checkout@v4

        - name: Run ShellCheck
          uses: ludeeus/action-shellcheck@master