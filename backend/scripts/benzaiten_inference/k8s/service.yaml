apiVersion: v1
kind: Service
metadata:
  name: benzaiten-inference-service
spec:
  type: LoadBalancer
  selector:
    app: benzaiten-inference
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080